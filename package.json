{
  "name": "votepit",
  "private": true,
  "type": "module",
  "description": "Votepit monorepo — React-SPA app/, Astro landing web/, shared @votepit/ui.",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
